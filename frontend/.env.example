# Frontend Environment Variables
# Copy this file to .env and fill in your values
# In Vite, all env vars exposed to the client must be prefixed with VITE_

# Default user ID used to filter dashboard data (internal DB user ID)
VITE_DEFAULT_USER_ID=

# Telegram user ID used when creating transactions from the web UI
VITE_TELEGRAM_USER_ID=
